import {html, css, nothing} from 'lit';

import {
  element,
  TiniElement,
  Colors,
  SubtleColors,
  Gradients,
  SubtleGradients,
  Sizes,
} from '@tinijs/core';

import {TiniXXXElement} from '../../ui/elements/xxx.js';

@element({
  elements: [TiniXXXElement],
})
export class AppPageUIDevXXXElement extends TiniElement {
  static readonly defaultTagName = 'app-page-ui-dev-xxx';

  protected render() {
    return html`
      <ui-dev-section titleText="-">
        <tini-xxx></tini-xxx>
      </ui-dev-section>
    `;
  }

  static styles = css``;
}
