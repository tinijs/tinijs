import {css} from 'lit';

import {defaultStyles} from '../../../elements/message.js';

export const styles = defaultStyles.extends({
  statics: css``,
  colorGen: () => '',
});

export default {styles};
